/* Combat System */

var player_combat = {
    isattack: 0, //is attacking or not
    cur_side: 0, //0 left, 1 top, 2 right
    stanima: 100 //energy the player still has left before recharge.

    //will reference player stats otherwise everything else will be modular
};